# Claude API Configuration
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Netlify Build Hook (optional)
# Create a build hook in Netlify: Settings > Build & deploy > Build hooks
# This allows GitHub Actions to trigger a rebuild after generating content
NETLIFY_BUILD_HOOK=your_netlify_build_hook_url_here

# Google Analytics (already configured in index.html)
# VITE_GA4_MEASUREMENT_ID is set directly in index.html
